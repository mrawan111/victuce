spring.application.name=victus-store
app.version=2.0.0

# ? PostgreSQL Database Connection
# Uses environment variables: SPRING_DATASOURCE_URL, SPRING_DATASOURCE_USERNAME, SPRING_DATASOURCE_PASSWORD
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/mydb}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Amenhaitham2@}
# ⭐ Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:detnafpfd}
cloudinary.api-key=${CLOUDINARY_API_KEY:484988919585589}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:JFIKFBH6Rik14NPbYdbk_V3d8IY}

# ⭐ File Upload Settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
# ?? Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ? Server Port
server.port=8080
server.address=0.0.0.0

# Production Features Configuration
app.idempotency.ttl-hours=24
app.refresh-token.expiration-days=7
app.version=2.0.0

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:8090,http://localhost:5173
app.cors.allow-credentials=true

# Logging Configuration (JSON structured logging)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - [%X{traceId}] - %level - %msg%n
logging.level.com.victusstore=INFO
logging.level.org.springframework.security=DEBUG

